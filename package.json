{
  "name": "pick-predict",
  "version": "0.4.0",
  "private": true,
  "description": "Live esports draft prediction game for co-streams and watch parties.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "extension:package": "node scripts/package-extension.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
